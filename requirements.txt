apscheduler>=3.10
ccxt>=4.5
pandas>=2.0
numpy>=1.24
pydantic>=2.0
pydantic-settings>=2.0
sqlalchemy>=2.0
websockets>=12.0       # Binance WS spot ticks
httpx>=0.27            # Polymarket CLOB HTTP client
anthropic>=0.45        # Claude decisions
twilio>=9.0            # WhatsApp (optional, prefer Telegram)
python-dotenv>=1.0
pytest>=8.0
streamlit>=1.30        # only used for local dev; prod dashboard is in crypto-trader
